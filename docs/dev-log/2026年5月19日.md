# 开发日志 - 2026-05-19

## 今日完成
- [x] 编写 README.md 和项目初始文档详情见doc目录下并提交到远程仓库
- [x] 项目初始化，建立模块化目录结构 并且使用.gitkeep 进行目录保持，让结构提交到仓库中
- [x] 初始化qt项目，将 Qt 默认生成的文件迁移至 `src/app` 和 `src/ui/views`
- [x] 配置CMake构建脚本，解决默认qt项目文件进行迁移后出现的头文件路径搜索问题

## 遇到的问题
- 文件迁移后，`main.cpp` 找不到 `mainwindow.h`，通过修改 CMakeLists.txt 添加 `target_include_directories` 解决