# 开发日志 - 2026-05-20

## 今日完成
- [x] **CMakeLists.txt 重构 (Step 1)**：新增 `Quick`、`QuickControls2`、`Sql`、`Network`、`WebSockets` 模块，支持 QML 编译。新增 `resources.qrc` 资源文件，移除已删除的 Widget 源文件引用，`target_link_libraries` 扩展为 6 个 Qt 模块
- [x] **QML 入口创建 (Step 2)**：新建 `src/ui/qml/main.qml`（ApplicationWindow 骨架），重写 `src/app/main.cpp`（切换为 `QQmlApplicationEngine` 加载 QML），新建 `resources.qrc` 资源文件（qrc:/qml/main.qml）
- [x] **清理 Widget 遗留代码**：删除 `src/ui/views/mainwindow.h`、`mainwindow.cpp`、`mainwindow.ui`（Qt Designer 表单），与设计文档要求的 QML 技术栈对齐
- [x] **目录补充**：创建 `src/ui/qml/` 目录，用于收纳未来所有 QML 视图文件
- [x] **开发规范文档 (009)**：新建 `docs/009.开发规范文档.md`，覆盖 Git 分支策略、Conventional Commits、C++/QML 代码命名规范、文档维护规则、CMake 构建规范、单人开发自律清单等 6 大部分，补齐 8 份设计文档未涉及的工程化空白
- [x] **C++ 分层骨架 (Step 3)**：新建 `src/core/base_service.h`（Service 基类 + 错误信号 + 权限校验接口）、`src/data/base_repository.h`（Repository 模板基类 + CRUD + 软删除 + 乐观锁）、`src/network/sync_manager.h`（SyncEntry 结构体 + SyncManager 类 + 指数退避常量）、`src/utils/constants.h`（主题色/枚举/活动系数/营养比/阈值/分页等全部业务常量），均已采用 `smart_diet` 命名空间和 Doxygen 注释；CMakeLists.txt 同步更新
- [x] **学习笔记体系搭建**：创建 `docs/study/` 目录与 `0000.学习笔记模板.md`，定义 10 大编号分区（Qt/C++/DB/网络/算法/安全/打包/性能/OCR）、笔记模板（背景→知识点→方案对比→项目应用→踩坑→参考）、索引表，用于后续自主学习记录
- [x] **学习笔记体系重构**：废弃按技术域编号（0100 系列），改为按开发任务编号（101=1.1/102=1.2/...），模板重构为「问题→思路→逻辑→方案→可行性→验证」六段式；拆分旧笔记为 `101.Qt项目CMake集成QML环境配置.md` + `102.分层架构骨架搭建与编译验证.md`

## 遇到的问题
- 无阻塞性问题。技术决策点：是否需要保留 Qt::Widgets 链接？结论：暂时保留，为后续系统托盘、文件对话框等 Widget 功能留接口

- [x] **编译验证 (Step 4)**：成功通过 `cmake --build`，零错误零警告。解决 3 个构建问题：① `BaseRepository<T>` 模板类不能用 `Q_OBJECT`（MOC 限制）→ 改为纯虚接口 ② MSVC 编译器缺少 `/permissive-` 和 `/utf-8` 标志 → 已添加 ③ Qt mingw_64 与 MSVC 编译器 ABI 不匹配 → 通过 `CMAKE_PREFIX_PATH` 指向 `msvc2022_64` 解决

## 明日计划
1. **Step 3 — C++ 分层骨架**（~3h）：编写 `baserepository.h`（Repository 抽象基类）、`baseservice.h`（Service 基类）、`syncmanager.h`（同步管理器桩）、`constants.h`（主题色/枚举常量）
2. **Step 4 — 编译验证**（~0.5h）：`cmake --build` 确认零错误、零警告，QML 窗口可正常启动

## 实际工时
__h / 计划 __h

## 备注
- 项目已从 QWidget 方向拨正到 QML + CMake + 分层架构轨道，与概要设计文档 v1.1 技术栈描述一致
- 下一步 1.3（数据库初始化）所需的 Qt SQL 模块已预置在 CMakeLists.txt 中
- [x] **DDL 建表脚本 (1.3.1)**：`resources/schema/sqlite_schema.sql`（23 张表 + 35+ 索引 + 外键） + `resources/schema/postgresql_schema.sql`（22 张表，类型适配 JSONB/BOOLEAN/TIMESTAMPTZ）
- [x] **DatabaseManager (1.3.2)**：`src/data/database_manager.h/.cpp`，单例模式，首次启动自动建表，WAL+外键 pragma，schema 版本追踪
- [x] **ORM 工具类 (1.3.3)**：`src/utils/uuid_utils.h`（UUID v4）、`src/utils/time_utils.h`（ISO 8601 UTC）、`src/data/orm_helper.h`（参数绑定 + 结果读取 + 存在性检查）
- [x] **UserRepository (1.3.4)**：`src/data/user_entity.h` + `user_repository.h/.cpp`，首个完整 Repository，实现 BaseRepository 全部 6 个接口 + 2 个登录查询，参数化查询防注入，乐观锁版本冲突检测
- [x] **学习笔记 (103)**：`docs/study/103.SQLite本地数据库与ORM映射框架搭建.md`，记录 DDL→ORM→Repository 全链路搭建过程

## 阶段 1 完成度

| 子任务 | 状态 | 提交数 |
|--------|:--:|:--:|
| 1.1 项目初始化 | ✅ | 1 |
| 1.2 架构搭建 | ✅ | 1 |
| 1.3 数据库初始化 | ✅ | 4 |
| 1.4 UI 基础组件 | ⬜ | 0 |
