# 开发日志 - 2026-05-20

## 今日完成
- [x] **CMakeLists.txt 重构 (Step 1)**：新增 `Quick`、`QuickControls2`、`Sql`、`Network`、`WebSockets` 模块，支持 QML 编译。新增 `resources.qrc` 资源文件，移除已删除的 Widget 源文件引用，`target_link_libraries` 扩展为 6 个 Qt 模块
- [x] **QML 入口创建 (Step 2)**：新建 `src/ui/qml/main.qml`（ApplicationWindow 骨架），重写 `src/app/main.cpp`（切换为 `QQmlApplicationEngine` 加载 QML），新建 `resources.qrc` 资源文件（qrc:/qml/main.qml）
- [x] **清理 Widget 遗留代码**：删除 `src/ui/views/mainwindow.h`、`mainwindow.cpp`、`mainwindow.ui`（Qt Designer 表单），与设计文档要求的 QML 技术栈对齐
- [x] **目录补充**：创建 `src/ui/qml/` 目录，用于收纳未来所有 QML 视图文件
- [x] **开发规范文档 (009)**：新建 `docs/009.开发规范文档.md`，覆盖 Git 分支策略、Conventional Commits、C++/QML 代码命名规范、文档维护规则、CMake 构建规范、单人开发自律清单等 6 大部分，补齐 8 份设计文档未涉及的工程化空白
- [x] **C++ 分层骨架 (Step 3)**：新建 `src/core/base_service.h`（Service 基类 + 错误信号 + 权限校验接口）、`src/data/base_repository.h`（Repository 模板基类 + CRUD + 软删除 + 乐观锁）、`src/network/sync_manager.h`（SyncEntry 结构体 + SyncManager 类 + 指数退避常量）、`src/utils/constants.h`（主题色/枚举/活动系数/营养比/阈值/分页等全部业务常量），均已采用 `smart_diet` 命名空间和 Doxygen 注释；CMakeLists.txt 同步更新
- [x] **学习笔记体系搭建**：创建 `docs/study/` 目录与 `0000.学习笔记模板.md`，定义 10 大编号分区（Qt/C++/DB/网络/算法/安全/打包/性能/OCR）、笔记模板（背景→知识点→方案对比→项目应用→踩坑→参考）、索引表，用于后续自主学习记录
- [x] **学习笔记体系重构**：废弃按技术域编号（0100 系列），改为按开发任务编号（101=1.1/102=1.2/...），模板重构为「问题→思路→逻辑→方案→可行性→验证」六段式；拆分旧笔记为 `101.Qt项目CMake集成QML环境配置.md` + `102.分层架构骨架搭建与编译验证.md`

## 遇到的问题
- 无阻塞性问题。技术决策点：是否需要保留 Qt::Widgets 链接？结论：暂时保留，为后续系统托盘、文件对话框等 Widget 功能留接口

- [x] **编译验证 (Step 4)**：成功通过 `cmake --build`，零错误零警告。解决 3 个构建问题：① `BaseRepository<T>` 模板类不能用 `Q_OBJECT`（MOC 限制）→ 改为纯虚接口 ② MSVC 编译器缺少 `/permissive-` 和 `/utf-8` 标志 → 已添加 ③ Qt mingw_64 与 MSVC 编译器 ABI 不匹配 → 通过 `CMAKE_PREFIX_PATH` 指向 `msvc2022_64` 解决

- [x] **注册页 UI (2.1.1)**：RegisterPage.qml（Step 1 表单）+ RegisterWindow.qml（独立窗口 420×680）+ BodyDataCard/GenderButton/GoalCard 三个可复用组件；修复 ScrollView 布局、parent.width 解析链、`visible:false` 缺漏、QML 分号语法等 5 个坑
- [x] **表单校验 (2.1.2)**：RegistrationValidator（C++ QObject），validate() 返回 {valid, errors}，6 条规则（昵称/身高/体重/年龄/性别/目标）
- [x] **TDEE 计算 (2.1.3)**：TdeeCalculator，Mifflin-St Jeor 公式（男+5 女-161），applyDietGoal（减脂×0.8/增肌×1.15）
- [x] **AuthService (2.1.4)**：注册流程编排（校验→唯一性→BMR→TDEE→SHA256→UserRepository.save），依赖注入链 DatabaseManager→UserRepo→AuthService→QML
- [x] **DatabaseManager 修复**：分号分割后注释跳过逻辑误吞 CREATE TABLE → 移除 startsWith("--") 跳过 + 纯注释块检测
- [x] **集成验证 (2.1.5)**：端到端注册成功（BMR/TDEE 正确），重复注册拦截正确
- [x] **2.1 任务对照**：计划书中「API 对接」未独立实现——因架构决策为离线优先，API=本地 Repository 调用；HTTP REST 推迟到阶段 8 与 WebSocket 同步一并落地
- [x] **登录 UI (2.2.1)**：LoginPage.qml（账号/密码/登录按钮/错误提示/去注册）+ LoginWindow.qml（420×480 独立窗口，子窗口弹出注册）
- [x] **AuthService::login() (2.2.2)**：查用户→验密码→返回结果；登录页/注册页独立窗口切换（hide/show）
- [x] **Session 管理 (2.2.3~2.2.4)**：SessionManager（QSettings 持久化），登录→关认证窗→加载 main.qml；启动→有会话直进主界面/无会话进登录；ProfilePage 退出登录（clearSession + Qt.quit）
- [x] **Argon2id 密码哈希 (2.3.1~2.3.3)**：集成 argon2 参考实现（静态库，C 编译），Argon2Hasher 封装（64MB/3轮/4并行/16B salt/32B hash），AuthService SHA-256→Argon2id，登录兼容旧密码并自动升级，修复 toBase64 填充导致 verify 失败
- [x] **Keychain (2.3.4)**：评估后推迟到阶段 10——QSettings 存 session 在当前桌面场景足够安全
- [x] **FamilyRepository (3.1.1)**：family_entity.h（Family/FamilyMember 实体）+ family_repository.h/.cpp（CRUD + getByInviteCode）+ family_member_repository.h/.cpp（addMember/removeMember/getByFamily）
- [x] **FamilyService (3.1.2)**：createFamily（6 位邀请码 + 设户主 owner）+ joinFamily（邀请码验证 + 去重）
- [x] **注册 Step 2 UI (3.1.3~3.1.4)**：FamilySetupPage（创建/加入 + 邀请码底部抽屉 Drawer），RegisterWindow 用 StackView 串联 Step1→Step2，Step1 先注册拿 userId → Step2 创建/加入家庭
- [x] **成员管理 (3.2)**：getMembers/getUserFamilies/setMemberPermissions/removeMember/transferOwnership；MemberListPage（成员列表+移交/移除+邀请码仅户主可见）；ProfilePage 重写（家庭列表+创建/加入新家庭入口）；FamilySetupPage 双模式（注册上一步/独立返回）
- [x] **家庭解散 (3.3)**：dissolveFamily 事务内 7 步级联混合删除（软删+物删），MemberListPage 红色解散按钮+二次确认；修复 wishlist_items 无 is_deleted → DELETE；修复 prepare/bindValue 参数不匹配 → arg() 拼接
- [x] **阶段 3 完成**：创建/加入 ✅ 成员管理 ✅ 解散 ✅
- [x] **食材库 (4.1)**：IngredientEntity + IngredientRepository + IngredientService（预置 10 种系统食材，种子数据）+ IngredientListPage/DetailPage
- [x] **菜谱库 (4.2)**：RecipeEntity + RecipeRepository + RecipeIngredientRepository + RecipeService（预置 3 道菜谱自动计算营养）+ RecipeListPage/DetailPage + RecipesPage 双入口
- [x] **菜谱创建/编辑/删除 (4.2.5~4.2.7)**：RecipeEditPage（表单+食材勾选+数量）+ 每100g营养归一化 + 编辑回填已有食材 + 软删除+列表自动刷新
- [x] **菜谱搜索+收藏 (4.3)**：RecipeListPage 搜索栏实时过滤 + RecipeFavoriteRepository + 详情页心形收藏按钮
- [x] **阶段 4 完成**：食材库 ✅ 菜谱库 ✅ 搜索+收藏 ✅
- [x] **开发计划更新 (008 v1.2)**：阶段 1/2 标记完成，远程任务归类到阶段 8，阶段 8 重写为三分结构

## 明日计划
1. **Step 3 — C++ 分层骨架**（~3h）：编写 `baserepository.h`（Repository 抽象基类）、`baseservice.h`（Service 基类）、`syncmanager.h`（同步管理器桩）、`constants.h`（主题色/枚举常量）
2. **Step 4 — 编译验证**（~0.5h）：`cmake --build` 确认零错误、零警告，QML 窗口可正常启动

## 实际工时
__h / 计划 __h

## 备注
- 项目已从 QWidget 方向拨正到 QML + CMake + 分层架构轨道，与概要设计文档 v1.1 技术栈描述一致
- 下一步 1.3（数据库初始化）所需的 Qt SQL 模块已预置在 CMakeLists.txt 中
- [x] **DDL 建表脚本 (1.3.1)**：`resources/schema/sqlite_schema.sql`（23 张表 + 35+ 索引 + 外键） + `resources/schema/postgresql_schema.sql`（22 张表，类型适配 JSONB/BOOLEAN/TIMESTAMPTZ）
- [x] **DatabaseManager (1.3.2)**：`src/data/database_manager.h/.cpp`，单例模式，首次启动自动建表，WAL+外键 pragma，schema 版本追踪
- [x] **ORM 工具类 (1.3.3)**：`src/utils/uuid_utils.h`（UUID v4）、`src/utils/time_utils.h`（ISO 8601 UTC）、`src/data/orm_helper.h`（参数绑定 + 结果读取 + 存在性检查）
- [x] **UserRepository (1.3.4)**：`src/data/user_entity.h` + `user_repository.h/.cpp`，首个完整 Repository，实现 BaseRepository 全部 6 个接口 + 2 个登录查询，参数化查询防注入，乐观锁版本冲突检测
- [x] **学习笔记 (103)**：`docs/study/103.SQLite本地数据库与ORM映射框架搭建.md`，记录 DDL→ORM→Repository 全链路搭建过程

## 阶段 1 完成度

| 子任务 | 状态 | 提交数 |
|--------|:--:|:--:|
| 1.1 项目初始化 | ✅ | 1 |
| 1.2 架构搭建 | ✅ | 1 |
| 1.3 数据库初始化 | ✅ | 4 |
| 1.4 UI 基础组件 | ✅ | 4 |
- [x] **Material Icons 集成**：下载 Google Material Icons Outlined 字体（339KB），封装 `Icon.qml` 组件（name/size/color），TabBar 和 4 Page 全部替换 emoji
- [x] **Icon.qml 属性冲突修复**：`property color color` 与 Text 内置属性同名 → QML 解析失败 → 删除自定义属性，直接用 Text.color
- [x] **主题单例 (1.4.1)**：`src/ui/styles/Theme.qml`（pragma Singleton，10 色 + 字号/间距/圆角/动效），`main.cpp` 注册 `SmartDiet.Style 1.0`
- [x] **主窗口框架 (1.4.2)**：StackView + 4 个 Page 占位（HomePage/FridgePage/RecipesPage/ProfilePage）
- [x] **底部导航栏 (1.4.3)**：TabBar + 4 TabButton（🏠首页/🧊冰箱/📖菜谱/👤我的），Tab 切换联动 StackView.replace
- [x] **学习笔记 (104)**：`docs/study/104.QML主题系统与底部导航框架搭建.md`

## 阶段 1 完成度

| 子任务 | 状态 | 提交数 |
|--------|:--:|:--:|
| 1.1 项目初始化 | ✅ | 1 |
| 1.2 架构搭建 | ✅ | 1 |
| 1.3 数据库初始化 | ✅ | 4 |
| 1.4 UI 基础组件 | ✅ | 3 |
